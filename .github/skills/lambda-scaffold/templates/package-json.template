{
  "name": "{{SERVICE_NAME}}",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/handlers/main.ts --bundle --platform=node --target=node20 --outdir=dist --external:@aws-sdk/*",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src/ tests/",
    "package": "npm run build && cd dist && zip -r lambda.zip ."
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.0.0",
    "@aws-sdk/client-ssm": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  }
}
